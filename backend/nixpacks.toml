[phases.setup]
nixPkgs = ["python311", "gdal", "geos", "proj", "postgresql_16.dev", "gcc"]

[phases.install]
cmds = ["pip install -r requirements_linux.txt"]

[start]
cmd = "python manage.py migrate --settings=config.settings.production && gunicorn config.wsgi:application --bind 0.0.0.0:$PORT"